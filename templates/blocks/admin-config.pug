extends ../admin-layout

block admin-header
    script(src='/scripts/admin-config.js')

block admin-content
    h2
        i.fas.fa-cogs.mr-5
        | Site Configurations
    section.section-container
        .col
            div
                span Site Status: 
                if config[1].status === 'Open'
                    span#site-status.user-badge.success-badge #{config[1].status}
                else
                    span#site-status.user-badge.critical-badge #{config[1].status}
            .admin-menu-container
                i.fas.fa-lg.fa-ellipsis-h.admin-menu-button
                .admin-menu
                    .site-config-option(data-config-id='2', data-status='Open') Open
                    .site-config-option(data-config-id='2', data-status='Closed') Close
        .col
            div
                span Registration Status: 
                if config[0].status === 'Open'
                    span#registration-status.user-badge.success-badge #{config[0].status}
                else
                    span#registration-status.user-badge.critical-badge #{config[0].status}
            .admin-menu-container
                i.fas.fa-lg.fa-ellipsis-h.admin-menu-button
                .admin-menu
                    .reg-config-option(data-config-id='1', data-status='Open') Open
                    .reg-config-option(data-config-id='1', data-status='Closed') Close